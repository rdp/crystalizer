test if :all is better than :built_in or whatever it is...hmm...hmm...

make gem work on coiso

benchmark it against 1.9, etc.

make it work on linux [?]

can it run rails? do I need to avoid binding/eval methods? benchmarks on rails?

it blows chunks?

it misses a method from IRB:

c:\installs\emacs-22.3\bin\emacs c:/ruby18/lib/ruby/1.8/irb/context.rb:219

    def evaluate(line, line_no)
      @line_no = line_no
      set_last_value(@workspace.evaluate(self, line, irb_path, line_no))
#      @workspace.evaluate("_ = IRB.conf[:MAIN_CONTEXT]._")
#      @_ = @workspace.evaluate(line, irb_path, line_no)
    end


